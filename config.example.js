// Copie para config.local.js apenas quando houver um adaptador remoto implementado.
// Nenhuma credencial é necessária ou utilizada nesta versão.
export const config = { storageProvider: "local", schemaVersion: 1 };
